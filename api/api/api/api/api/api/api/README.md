# BROKEN LORD MEGA AI API 👑

Endpoints:
- GET  `/api`                → Welcome
- POST `/api/chat`           → Chat
- POST `/api/generate-image` → Image generation
- POST `/api/generate-audio` → Text-to-speech
- POST `/api/generate-video` → Video generation
- POST `/api/embeddings`     → Embeddings
- POST `/api/moderation`     → Moderation
- POST `/api/all-in-one`     → Multi-mode

Env:
OPENAI_API_KEY=your_key_here
